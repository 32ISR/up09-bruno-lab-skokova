info:
  name: spisok
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/items/:id/bids"
  params:
    - name: id
      value: "1766396704038"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
