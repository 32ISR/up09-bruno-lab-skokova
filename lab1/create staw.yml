info:
  name: create staw
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/items/:id/bids"
  params:
    - name: id
      value: "1761624428511"
      type: path
  body:
    type: json
    data: |-
      {
        "amount": 85000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
